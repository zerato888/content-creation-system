# Direct Python dependencies of the core. Pinned; the full hash-pinned closure
# lives in requirements.lock, generated by `python tools/lock_deps.py`.
# Installed with: pip install --require-hashes --no-deps -r requirements.lock  (into .kit/venv)
faster-whisper==1.2.1   # captions, Command Center transcription
playwright==1.63.0      # carousel-news PNG; browsers go to the shared user cache
tzdata==2026.4          # IANA timezones on Windows (no system database): onboarding + Command Center
yt-dlp==2026.8.19       # Command Center ingest, reference-clone

# numpy pinned so one lock covers CPython 3.11-3.13
numpy==2.4.6
onnxruntime==1.23.2
